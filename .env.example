# Server Configuration
PORT=3000
SERVER_ADDRESS=localhost

# Database Configuration
# Set to false to run without database (uses client-side import/export only)
DB_ENABLED=true
DB_TYPE=mongodb

# MongoDB Configuration (Local)
# For local development without authentication:
MONGODB_URI=mongodb://sovuser:sovpass123@localhost:27017
MONGODB_DB_NAME=sovereignty_db

# MongoDB Atlas Configuration (Cloud)
# Uncomment and configure to use MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
# MONGODB_DB_NAME=sovereignty_db